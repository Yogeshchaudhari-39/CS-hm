spring:
  application:
    name: assignments
  datasource:
    username: root
    password: root@123
    url: jdbc:mysql://localhost:3306/learning
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true